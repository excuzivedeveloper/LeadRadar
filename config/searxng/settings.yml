use_default_settings:
  engines:
    remove:
      - brave
      - duckduckgo
      - startpage

server:
  bind_address: 0.0.0.0
  port: 8080
  limiter: false
  secret_key: local-development-searxng-key

search:
  formats:
    - html
    - json
